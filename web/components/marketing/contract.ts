// The landing page's direction contract, rendered as a literal HTML comment
// (via dangerouslySetInnerHTML, since JSX {/* comments */} are compiled away
// and never reach the DOM) so it survives the production build and can be
// audited by grepping the shipped HTML for "CONTRACT".
export const CONTRACT = `<!-- CONTRACT
THESIS: Permission as a taped boundary. The category ships a dashboard
screenshot behind a gradient blob; this refuses that by never showing
software chrome at all — only a mocked homepage with real masking tape laid
across the parts a client may never touch.
OWN-WORLD: raw canvas ground, near-black ink, sign-paint yellow as the
"editable" wash, a torn-edge tape motif (clip-path zigzag, per-strip
rotation) reused for every locked boundary on the page. Alfa Slab One for
stencilled headlines, Caveat for hand labels only, Archivo for body.
STORY: a client can repaint exactly what's taped as theirs — words, one
photo, a button — and nothing else, no matter how hard they pull at it.
FIRST VIEWPORT: nav with a stamp-mark logo; a mocked homepage panel with
three tape strips over layout/structure/code and yellow-washed headline,
photo and button regions; headline and CTA beside it.
FORM: Taped Line — sign-painter's masking tape & stencil work, position 4 of
7 grounded directions (Redline Set, Access Control, Suggesting Mode, Taped
Line, Permit Office, Contact Sheet, Gallery Line). Seed key afd8eb7f.
FINISH: unreviewed and undocumented is unfinished; this build ends with the
finish review, the verdict, and DESIGN.md.
-->`;
